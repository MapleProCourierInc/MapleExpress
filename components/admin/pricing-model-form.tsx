"use client"

import { ArrowDown, ArrowUp, Plus, Trash2 } from "lucide-react"
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Switch } from "@/components/ui/switch"
import { Textarea } from "@/components/ui/textarea"
import type { CreatePricingV2Request, PricingV2DistanceSlab, PricingV2PackageSlab, PricingV2Surcharge, PricingV2Tax } from "@/types/pricing"

export const quoteSafeTemplate: CreatePricingV2Request = {
  name: "Standard Global Pricing", description: "Default pricing for standard courier deliveries", status: "DRAFT", pricingType: "GLOBAL", ownerId: null,
  zoneCode: "GLOBAL", zoneDisplayName: "Global", currency: "CAD", effectiveFrom: null, effectiveTo: null,
  dimensionalWeight: { enabled: true, displayName: "Dimensional Weight", unit: "CM", divisor: 5000, roundingScale: 2, roundingMode: "CEILING" },
  chargeableWeight: { enabled: true, displayName: "Chargeable Weight", calculationType: "MAX_OF_ACTUAL_AND_DIMENSIONAL" },
  packageSlabs: [
    { slabCode: "SMALL", displayName: "Small Package", enabled: true, tierOrder: 1, minChargeableWeightKgExclusive: null, maxChargeableWeightKg: 5, minDimensionSumCmExclusive: null, maxDimensionSumCm: 60, basePrice: 9.99 },
    { slabCode: "MEDIUM", displayName: "Medium Package", enabled: true, tierOrder: 2, minChargeableWeightKgExclusive: 5, maxChargeableWeightKg: 10, minDimensionSumCmExclusive: 60, maxDimensionSumCm: 120, basePrice: 14.99 },
    { slabCode: "LARGE", displayName: "Large Package", enabled: true, tierOrder: 3, minChargeableWeightKgExclusive: 10, maxChargeableWeightKg: 25, minDimensionSumCmExclusive: 120, maxDimensionSumCm: 200, basePrice: 19.99 },
  ],
  distancePricing: { enabled: true, displayName: "Distance Pricing", includedDistanceKm: 10, distanceSlabs: [
    { slabCode: "DISTANCE_10_25", displayName: "Distance Charge 10-25 KM", enabled: true, fromKmExclusive: 10, toKmInclusive: 25, calculationType: "PER_KM", rate: 0.75 },
    { slabCode: "DISTANCE_25_50", displayName: "Distance Charge 25-50 KM", enabled: true, fromKmExclusive: 25, toKmInclusive: 50, calculationType: "PER_KM", rate: 1 },
  ] },
  surcharges: [
    { surchargeCode: "RUSH_PRIORITY", displayName: "Rush / Priority Fee", description: "Applied when rush delivery is requested", enabled: true, triggerType: "SERVICE_TYPE", triggerValue: "RUSH_PRIORITY", startTime: null, endTime: null, timezone: null, calculationType: "FLAT", amount: 5, percentage: null, percentageBase: null },
    { surchargeCode: "AFTER_HOURS", displayName: "After Hours Fee", description: "Applied during the configured Halifax time window", enabled: true, triggerType: "TIME_WINDOW", triggerValue: null, startTime: "17:00:00", endTime: "21:00:00", timezone: "America/Halifax", calculationType: "FLAT", amount: 5, percentage: null, percentageBase: null },
  ],
  taxes: [{ taxCode: "HST", displayName: "HST", enabled: true, calculationType: "PERCENTAGE", percentage: 15, percentageBase: "SUBTOTAL" }],
  customQuoteRules: { enabled: true, maxStandardDistanceKm: 50, distanceExceededMessage: "Distance exceeds standard pricing limits. Please contact us for a manual quote." },
  rounding: { moneyScale: 2, moneyRoundingMode: "HALF_UP", measurementScale: 2, measurementRoundingMode: "CEILING" },
}

export const freshPricingTemplate = () => structuredClone(quoteSafeTemplate)
const numberValue = (value: string) => value === "" ? 0 : Number(value)
const nullableNumber = (value: string) => value === "" ? null : Number(value)
const textOrNull = (value: string) => value.trim() ? value : null

function Field({ label, children, hint }: { label: string; children: React.ReactNode; hint?: string }) {
  return <div className="space-y-1.5"><Label>{label}</Label>{children}{hint && <p className="text-xs text-muted-foreground">{hint}</p>}</div>
}
function Toggle({ label, checked, onCheckedChange }: { label: string; checked: boolean; onCheckedChange: (checked: boolean) => void }) {
  return <div className="flex items-center gap-2"><Switch checked={checked} onCheckedChange={onCheckedChange} /><Label>{label}</Label></div>
}
function NumberInput({ value, onChange, nullable = false }: { value: number | null; onChange: (value: number | null) => void; nullable?: boolean }) {
  return <Input type="number" step="any" value={value ?? ""} onChange={(event) => onChange(nullable ? nullableNumber(event.target.value) : numberValue(event.target.value))} />
}
function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <div className="space-y-4 rounded-md border p-4"><h4 className="font-medium">{title}</h4>{children}</div> }

export function PricingModelForm({ value, onChange }: { value: CreatePricingV2Request; onChange: (value: CreatePricingV2Request) => void }) {
  const update = (mutate: (draft: CreatePricingV2Request) => void) => { const draft = structuredClone(value); mutate(draft); onChange(draft) }
  const setPackage = (index: number, mutate: (slab: PricingV2PackageSlab) => void) => update((draft) => mutate(draft.packageSlabs[index]))
  const setDistance = (index: number, mutate: (slab: PricingV2DistanceSlab) => void) => update((draft) => mutate(draft.distancePricing.distanceSlabs[index]))
  const setSurcharge = (index: number, mutate: (rule: PricingV2Surcharge) => void) => update((draft) => mutate(draft.surcharges[index]))
  const setTax = (index: number, mutate: (tax: PricingV2Tax) => void) => update((draft) => mutate(draft.taxes[index]))
  return <Accordion type="multiple" defaultValue={["basics", "packages"]} className="space-y-2">
    <AccordionItem value="basics"><AccordionTrigger>1. Model details and scope</AccordionTrigger><AccordionContent className="space-y-4">
      <div className="grid gap-4 md:grid-cols-2"><Field label="Model name"><Input value={value.name} onChange={(e) => update((d) => { d.name = e.target.value })} /></Field><Field label="Currency"><Input value={value.currency} onChange={(e) => update((d) => { d.currency = e.target.value.toUpperCase() })} /></Field></div>
      <Field label="Description"><Textarea value={value.description || ""} onChange={(e) => update((d) => { d.description = textOrNull(e.target.value) })} /></Field>
      <div className="grid gap-4 md:grid-cols-3"><Field label="Pricing scope"><Select value={value.pricingType} onValueChange={(v: "GLOBAL" | "CUSTOMER_SPECIFIC") => update((d) => { d.pricingType = v; if (v === "GLOBAL") d.ownerId = null })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="GLOBAL">Global</SelectItem><SelectItem value="CUSTOMER_SPECIFIC">Customer-specific</SelectItem></SelectContent></Select></Field><Field label="Zone code"><Input value={value.zoneCode} onChange={(e) => update((d) => { d.zoneCode = e.target.value })} /></Field><Field label="Zone display name"><Input value={value.zoneDisplayName || ""} onChange={(e) => update((d) => { d.zoneDisplayName = textOrNull(e.target.value) })} /></Field></div>
      {value.pricingType === "CUSTOMER_SPECIFIC" && <Field label="Customer owner ID" hint="Use the customer's JWT subject ID."><Input value={value.ownerId || ""} onChange={(e) => update((d) => { d.ownerId = textOrNull(e.target.value) })} /></Field>}
    </AccordionContent></AccordionItem>
    <AccordionItem value="weight"><AccordionTrigger>2. Dimensional and chargeable weight</AccordionTrigger><AccordionContent className="space-y-4"><Toggle label="Calculate dimensional weight" checked={value.dimensionalWeight.enabled} onCheckedChange={(v) => update((d) => { d.dimensionalWeight.enabled = v })} /><div className="grid gap-4 md:grid-cols-4"><Field label="Display name"><Input value={value.dimensionalWeight.displayName || ""} onChange={(e) => update((d) => { d.dimensionalWeight.displayName = textOrNull(e.target.value) })} /></Field><Field label="Dimension unit"><Select value={value.dimensionalWeight.unit} onValueChange={(v: "CM" | "INCH") => update((d) => { d.dimensionalWeight.unit = v })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="CM">Centimeters</SelectItem><SelectItem value="INCH">Inches</SelectItem></SelectContent></Select></Field><Field label="Divisor"><NumberInput value={value.dimensionalWeight.divisor} onChange={(v) => update((d) => { d.dimensionalWeight.divisor = v || 0 })} /></Field><Field label="Rounding scale"><NumberInput value={value.dimensionalWeight.roundingScale} onChange={(v) => update((d) => { d.dimensionalWeight.roundingScale = v || 0 })} /></Field></div><div className="grid gap-4 md:grid-cols-2"><Field label="Dimensional rounding"><RoundingSelect value={value.dimensionalWeight.roundingMode} onChange={(v) => update((d) => { d.dimensionalWeight.roundingMode = v })} /></Field><Field label="Chargeable weight"><Toggle label="Use the greater of actual and dimensional weight" checked={value.chargeableWeight.enabled} onCheckedChange={(v) => update((d) => { d.chargeableWeight.enabled = v })} /></Field><Field label="Chargeable weight display name"><Input value={value.chargeableWeight.displayName || ""} onChange={(e) => update((d) => { d.chargeableWeight.displayName = textOrNull(e.target.value) })} /></Field></div></AccordionContent></AccordionItem>
    <AccordionItem value="packages"><AccordionTrigger>3. Package pricing slabs ({value.packageSlabs.length})</AccordionTrigger><AccordionContent className="space-y-3"><p className="text-xs text-muted-foreground">Ranges are lower-exclusive and upper-inclusive. Keep enabled tiers sequential without gaps.</p>{value.packageSlabs.map((slab, index) => <Panel key={index} title={`Tier ${index + 1}: ${slab.displayName || "New package slab"}`}><div className="flex justify-between"><Toggle label="Enabled" checked={slab.enabled} onCheckedChange={(v) => setPackage(index, (s) => { s.enabled = v })} /><Button variant="ghost" size="sm" onClick={() => update((d) => { d.packageSlabs.splice(index, 1) })}><Trash2 className="mr-1 h-4 w-4" />Remove</Button></div><div className="grid gap-3 md:grid-cols-3"><Field label="Slab code"><Input value={slab.slabCode} onChange={(e) => setPackage(index, (s) => { s.slabCode = e.target.value })} /></Field><Field label="Display name"><Input value={slab.displayName} onChange={(e) => setPackage(index, (s) => { s.displayName = e.target.value })} /></Field><Field label="Tier order"><NumberInput value={slab.tierOrder} onChange={(v) => setPackage(index, (s) => { s.tierOrder = v || 0 })} /></Field><Field label="Min weight kg (exclusive)"><NumberInput nullable value={slab.minChargeableWeightKgExclusive} onChange={(v) => setPackage(index, (s) => { s.minChargeableWeightKgExclusive = v })} /></Field><Field label="Max weight kg"><NumberInput nullable value={slab.maxChargeableWeightKg} onChange={(v) => setPackage(index, (s) => { s.maxChargeableWeightKg = v })} /></Field><Field label="Base price"><NumberInput value={slab.basePrice} onChange={(v) => setPackage(index, (s) => { s.basePrice = v || 0 })} /></Field><Field label="Min dimension sum cm (exclusive)"><NumberInput nullable value={slab.minDimensionSumCmExclusive} onChange={(v) => setPackage(index, (s) => { s.minDimensionSumCmExclusive = v })} /></Field><Field label="Max dimension sum cm"><NumberInput nullable value={slab.maxDimensionSumCm} onChange={(v) => setPackage(index, (s) => { s.maxDimensionSumCm = v })} /></Field></div></Panel>)}<Button variant="outline" onClick={() => update((d) => { d.packageSlabs.push({ slabCode: "", displayName: "", enabled: true, tierOrder: d.packageSlabs.length + 1, minChargeableWeightKgExclusive: null, maxChargeableWeightKg: null, minDimensionSumCmExclusive: null, maxDimensionSumCm: null, basePrice: 0 }) })}><Plus className="mr-2 h-4 w-4" />Add package slab</Button></AccordionContent></AccordionItem>
    <AccordionItem value="distance"><AccordionTrigger>4. Distance pricing ({value.distancePricing.distanceSlabs.length} slabs)</AccordionTrigger><AccordionContent className="space-y-3"><Toggle label="Enable distance pricing" checked={value.distancePricing.enabled} onCheckedChange={(v) => update((d) => { d.distancePricing.enabled = v })} /><div className="grid gap-4 md:grid-cols-2"><Field label="Display name"><Input value={value.distancePricing.displayName || ""} onChange={(e) => update((d) => { d.distancePricing.displayName = textOrNull(e.target.value) })} /></Field><Field label="Included distance km"><NumberInput value={value.distancePricing.includedDistanceKm} onChange={(v) => update((d) => { d.distancePricing.includedDistanceKm = v || 0 })} /></Field></div>{value.distancePricing.distanceSlabs.map((slab, index) => <Panel key={index} title={`Distance slab ${index + 1}: ${slab.displayName || "New distance slab"}`}><div className="flex justify-between"><Toggle label="Enabled" checked={slab.enabled} onCheckedChange={(v) => setDistance(index, (s) => { s.enabled = v })} /><Button variant="ghost" size="sm" onClick={() => update((d) => { d.distancePricing.distanceSlabs.splice(index, 1) })}><Trash2 className="mr-1 h-4 w-4" />Remove</Button></div><div className="grid gap-3 md:grid-cols-3"><Field label="Slab code"><Input value={slab.slabCode} onChange={(e) => setDistance(index, (s) => { s.slabCode = e.target.value })} /></Field><Field label="Display name"><Input value={slab.displayName} onChange={(e) => setDistance(index, (s) => { s.displayName = e.target.value })} /></Field><Field label="Calculation"><Select value={slab.calculationType} onValueChange={(v: "FLAT" | "PER_KM") => setDistance(index, (s) => { s.calculationType = v })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="PER_KM">Per kilometer</SelectItem><SelectItem value="FLAT">Flat</SelectItem></SelectContent></Select></Field><Field label="From km (exclusive)"><NumberInput nullable value={slab.fromKmExclusive} onChange={(v) => setDistance(index, (s) => { s.fromKmExclusive = v })} /></Field><Field label="To km (inclusive)"><NumberInput nullable value={slab.toKmInclusive} onChange={(v) => setDistance(index, (s) => { s.toKmInclusive = v })} /></Field><Field label="Rate"><NumberInput value={slab.rate} onChange={(v) => setDistance(index, (s) => { s.rate = v || 0 })} /></Field></div></Panel>)}<Button variant="outline" onClick={() => update((d) => { d.distancePricing.distanceSlabs.push({ slabCode: "", displayName: "", enabled: true, fromKmExclusive: null, toKmInclusive: null, calculationType: "PER_KM", rate: 0 }) })}><Plus className="mr-2 h-4 w-4" />Add distance slab</Button></AccordionContent></AccordionItem>
    <AccordionItem value="surcharges"><AccordionTrigger>5. Surcharges ({value.surcharges.length})</AccordionTrigger><AccordionContent className="space-y-3"><p className="text-xs text-muted-foreground">Percentage surcharges apply in list order. Time windows are currently evaluated in America/Halifax.</p>{value.surcharges.map((rule, index) => <Panel key={index} title={`Surcharge ${index + 1}: ${rule.displayName || "New surcharge"}`}><div className="flex justify-between"><Toggle label="Enabled" checked={rule.enabled} onCheckedChange={(v) => setSurcharge(index, (s) => { s.enabled = v })} /><div className="flex gap-1"><Button variant="ghost" size="icon" disabled={index === 0} aria-label="Move surcharge up" onClick={() => update((d) => { const [rule] = d.surcharges.splice(index, 1); d.surcharges.splice(index - 1, 0, rule) })}><ArrowUp className="h-4 w-4" /></Button><Button variant="ghost" size="icon" disabled={index === value.surcharges.length - 1} aria-label="Move surcharge down" onClick={() => update((d) => { const [rule] = d.surcharges.splice(index, 1); d.surcharges.splice(index + 1, 0, rule) })}><ArrowDown className="h-4 w-4" /></Button><Button variant="ghost" size="sm" onClick={() => update((d) => { d.surcharges.splice(index, 1) })}><Trash2 className="mr-1 h-4 w-4" />Remove</Button></div></div><div className="grid gap-3 md:grid-cols-3"><Field label="Code"><Input value={rule.surchargeCode} onChange={(e) => setSurcharge(index, (s) => { s.surchargeCode = e.target.value })} /></Field><Field label="Display name"><Input value={rule.displayName} onChange={(e) => setSurcharge(index, (s) => { s.displayName = e.target.value })} /></Field><Field label="Trigger"><Select value={rule.triggerType} onValueChange={(v: PricingV2Surcharge["triggerType"]) => setSurcharge(index, (s) => { s.triggerType = v; s.timezone = v === "TIME_WINDOW" ? "America/Halifax" : null })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="ALWAYS">Always</SelectItem><SelectItem value="SERVICE_TYPE">Service type</SelectItem><SelectItem value="TIME_WINDOW">Time window</SelectItem></SelectContent></Select></Field><Field label="Calculation"><Select value={rule.calculationType} onValueChange={(v: PricingV2Surcharge["calculationType"]) => setSurcharge(index, (s) => { s.calculationType = v; if (v === "PERCENTAGE" && !s.percentageBase) s.percentageBase = "SUBTOTAL" })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="FLAT">Flat</SelectItem><SelectItem value="PERCENTAGE">Percentage</SelectItem></SelectContent></Select></Field>{rule.calculationType === "FLAT" ? <Field label="Amount"><NumberInput nullable value={rule.amount} onChange={(v) => setSurcharge(index, (s) => { s.amount = v })} /></Field> : <><Field label="Percentage"><NumberInput nullable value={rule.percentage} onChange={(v) => setSurcharge(index, (s) => { s.percentage = v })} /></Field><Field label="Percentage base"><PercentageBaseSelect value={rule.percentageBase || "SUBTOTAL"} onChange={(v) => setSurcharge(index, (s) => { s.percentageBase = v })} /></Field></>}{rule.triggerType === "SERVICE_TYPE" && <Field label="Service type value"><Input value={rule.triggerValue || ""} onChange={(e) => setSurcharge(index, (s) => { s.triggerValue = textOrNull(e.target.value) })} /></Field>}{rule.triggerType === "TIME_WINDOW" && <><Field label="Start time"><Input type="time" step="1" value={rule.startTime || ""} onChange={(e) => setSurcharge(index, (s) => { s.startTime = textOrNull(e.target.value) })} /></Field><Field label="End time"><Input type="time" step="1" value={rule.endTime || ""} onChange={(e) => setSurcharge(index, (s) => { s.endTime = textOrNull(e.target.value) })} /></Field><Field label="Timezone" hint="The quote service currently evaluates time windows in Halifax time."><Input value="America/Halifax" disabled /></Field></>}</div><Field label="Description"><Input value={rule.description || ""} onChange={(e) => setSurcharge(index, (s) => { s.description = textOrNull(e.target.value) })} /></Field></Panel>)}<Button variant="outline" onClick={() => update((d) => { d.surcharges.push({ surchargeCode: "", displayName: "", description: null, enabled: true, triggerType: "ALWAYS", triggerValue: null, startTime: null, endTime: null, timezone: null, calculationType: "FLAT", amount: 0, percentage: null, percentageBase: null }) })}><Plus className="mr-2 h-4 w-4" />Add surcharge</Button></AccordionContent></AccordionItem>
    <AccordionItem value="taxes"><AccordionTrigger>6. Taxes ({value.taxes.length})</AccordionTrigger><AccordionContent className="space-y-3">{value.taxes.map((tax, index) => <Panel key={index} title={`Tax ${index + 1}: ${tax.displayName || "New tax"}`}><div className="flex justify-between"><Toggle label="Enabled" checked={tax.enabled} onCheckedChange={(v) => setTax(index, (t) => { t.enabled = v })} /><Button variant="ghost" size="sm" onClick={() => update((d) => { d.taxes.splice(index, 1) })}><Trash2 className="mr-1 h-4 w-4" />Remove</Button></div><div className="grid gap-3 md:grid-cols-3"><Field label="Tax code"><Input value={tax.taxCode} onChange={(e) => setTax(index, (t) => { t.taxCode = e.target.value })} /></Field><Field label="Display name"><Input value={tax.displayName} onChange={(e) => setTax(index, (t) => { t.displayName = e.target.value })} /></Field><Field label="Percentage"><NumberInput value={tax.percentage} onChange={(v) => setTax(index, (t) => { t.percentage = v || 0 })} /></Field><Field label="Percentage base"><PercentageBaseSelect value={tax.percentageBase} onChange={(v) => setTax(index, (t) => { t.percentageBase = v })} /></Field></div></Panel>)}<Button variant="outline" onClick={() => update((d) => { d.taxes.push({ taxCode: "", displayName: "", enabled: true, calculationType: "PERCENTAGE", percentage: 0, percentageBase: "SUBTOTAL" }) })}><Plus className="mr-2 h-4 w-4" />Add tax</Button></AccordionContent></AccordionItem>
    <AccordionItem value="advanced"><AccordionTrigger>7. Custom quotes and rounding</AccordionTrigger><AccordionContent className="space-y-4">{value.customQuoteRules && <><Toggle label="Require custom quotes beyond a maximum distance" checked={value.customQuoteRules.enabled} onCheckedChange={(v) => update((d) => { if (d.customQuoteRules) d.customQuoteRules.enabled = v })} /><Field label="Maximum standard distance km"><NumberInput nullable value={value.customQuoteRules.maxStandardDistanceKm} onChange={(v) => update((d) => { if (d.customQuoteRules) d.customQuoteRules.maxStandardDistanceKm = v })} /></Field><Field label="Distance exceeded message"><Textarea value={value.customQuoteRules.distanceExceededMessage || ""} onChange={(e) => update((d) => { if (d.customQuoteRules) d.customQuoteRules.distanceExceededMessage = textOrNull(e.target.value) })} /></Field></>}<div className="grid gap-3 md:grid-cols-4"><Field label="Money decimals"><NumberInput value={value.rounding.moneyScale} onChange={(v) => update((d) => { d.rounding.moneyScale = v || 0 })} /></Field><Field label="Money rounding"><RoundingSelect value={value.rounding.moneyRoundingMode} onChange={(v) => update((d) => { d.rounding.moneyRoundingMode = v })} /></Field><Field label="Measurement decimals"><NumberInput value={value.rounding.measurementScale} onChange={(v) => update((d) => { d.rounding.measurementScale = v || 0 })} /></Field><Field label="Measurement rounding"><RoundingSelect value={value.rounding.measurementRoundingMode} onChange={(v) => update((d) => { d.rounding.measurementRoundingMode = v })} /></Field></div></AccordionContent></AccordionItem>
  </Accordion>
}

function RoundingSelect({ value, onChange }: { value: CreatePricingV2Request["rounding"]["moneyRoundingMode"]; onChange: (value: "HALF_UP" | "CEILING" | "FLOOR") => void }) { return <Select value={value} onValueChange={onChange}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="HALF_UP">Half up</SelectItem><SelectItem value="CEILING">Ceiling</SelectItem><SelectItem value="FLOOR">Floor</SelectItem></SelectContent></Select> }

function PercentageBaseSelect({ value, onChange }: { value: "SUBTOTAL" | "BASE_PACKAGE_PRICE" | "DISTANCE_CHARGE" | "PRE_TAX_TOTAL"; onChange: (value: "SUBTOTAL" | "BASE_PACKAGE_PRICE" | "DISTANCE_CHARGE" | "PRE_TAX_TOTAL") => void }) { return <Select value={value} onValueChange={onChange}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="SUBTOTAL">Subtotal</SelectItem><SelectItem value="BASE_PACKAGE_PRICE">Base package price</SelectItem><SelectItem value="DISTANCE_CHARGE">Distance charge</SelectItem><SelectItem value="PRE_TAX_TOTAL">Pre-tax total</SelectItem></SelectContent></Select> }
